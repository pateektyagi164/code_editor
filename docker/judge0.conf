################################################################################
# Judge0 — used with: docker compose --profile judge0 up -d
################################################################################

REDIS_HOST=judge0-redis
REDIS_PORT=6379
REDIS_PASSWORD=codecollab_judge0_redis

POSTGRES_HOST=judge0-db
POSTGRES_PORT=5432
POSTGRES_DB=judge0
POSTGRES_USER=judge0
POSTGRES_PASSWORD=codecollab_judge0_pg

RAILS_ENV=production
